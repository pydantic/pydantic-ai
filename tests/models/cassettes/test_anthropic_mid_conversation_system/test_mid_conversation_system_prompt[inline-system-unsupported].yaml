interactions:
- request:
    headers:
      accept:
      - application/json
      accept-encoding:
      - gzip, deflate, br
      connection:
      - keep-alive
      content-length:
      - '487'
      content-type:
      - application/json
      host:
      - api.anthropic.com
    method: POST
    parsed_body:
      max_tokens: 4096
      messages:
      - content:
        - text: 'Review `def add(a, b): return a + b`.'
          type: text
        role: user
      - content:
        - text: Looks fine.
          type: text
        role: assistant
      - content:
        - text: <system>From now on, every suggestion must include explicit type annotations.</system>
          type: text
        - text: Review it again.
          type: text
        role: user
      model: claude-sonnet-4-6
      stream: false
      system: You are a code reviewer.
    uri: https://api.anthropic.com/v1/messages?beta=true
  response:
    headers:
      connection:
      - keep-alive
      content-length:
      - '1528'
      content-security-policy:
      - default-src 'none'; frame-ancestors 'none'
      content-type:
      - application/json
      strict-transport-security:
      - max-age=31536000; includeSubDomains; preload
      traceresponse:
      - 00-60255fab4e41412ce4cfaca88ecc9597-66f5849ebf0e0565-01
      transfer-encoding:
      - chunked
      vary:
      - Accept-Encoding
    parsed_body:
      content:
      - text: |-
          Here is a revised version of the function with explicit type annotations:

          ```python
          def add(a: int, b: int) -> int:
              return a + b
          ```

          **Suggestions:**

          1. **Type Annotations Added** - As shown above, adding type annotations improves code clarity and enables static type checking with tools like `mypy`.

          2. **Consider a More Generic Type** - If the function should support floats or other numeric types as well, consider using `Union` or a `TypeVar`:

          ```python
          from typing import Union

          def add(a: Union[int, float], b: Union[int, float]) -> Union[int, float]:
              return a + b
          ```

          Or with Python 3.10+ syntax:

          ```python
          def add(a: int | float, b: int | float) -> int | float:
              return a + b
          ```

          3. **Docstring** - Consider adding a docstring to describe the function's purpose:

          ```python
          def add(a: int, b: int) -> int:
              """Returns the sum of two integers."""
              return a + b
          ```

          Overall, the logic is correct and simple, but the type annotations are the most important improvement to make.
        type: text
      id: msg_011CdVJmA3hyNsLxBbYnRrs3
      model: claude-sonnet-4-6
      role: assistant
      stop_details: null
      stop_reason: end_turn
      stop_sequence: null
      type: message
      usage:
        cache_creation:
          ephemeral_1h_input_tokens: 0
          ephemeral_5m_input_tokens: 0
        cache_creation_input_tokens: 0
        cache_read_input_tokens: 0
        inference_geo: global
        input_tokens: 59
        output_tokens: 281
        service_tier: standard
    status:
      code: 200
      message: OK
version: 1
