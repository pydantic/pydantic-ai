interactions:
- request:
    headers:
      accept:
      - application/json
      accept-encoding:
      - gzip, deflate, br
      connection:
      - keep-alive
      content-length:
      - '487'
      content-type:
      - application/json
      host:
      - api.anthropic.com
    method: POST
    parsed_body:
      max_tokens: 4096
      messages:
      - content:
        - text: 'Review `def add(a, b): return a + b`.'
          type: text
        role: user
      - content:
        - text: Looks fine.
          type: text
        role: assistant
      - content:
        - text: <system>From now on, every suggestion must include explicit type annotations.</system>
          type: text
        - text: Review it again.
          type: text
        role: user
      model: claude-sonnet-4-6
      stream: false
      system: You are a code reviewer.
    uri: https://api.anthropic.com/v1/messages?beta=true
  response:
    headers:
      connection:
      - keep-alive
      content-length:
      - '1576'
      content-security-policy:
      - default-src 'none'; frame-ancestors 'none'
      content-type:
      - application/json
      strict-transport-security:
      - max-age=31536000; includeSubDomains; preload
      traceresponse:
      - 00-fd8d14ccd7290d228d8d8093c0954552-537f6b6a5c2d713f-01
      transfer-encoding:
      - chunked
      vary:
      - Accept-Encoding
    parsed_body:
      content:
      - text: |-
          Here's a review of the function with type annotation suggestions:

          **Current Code:**
          ```python
          def add(a, b):
              return a + b
          ```

          **Issues & Suggestions:**

          1. **Missing Type Annotations** - Add explicit type annotations to clarify expected input and output types:
          ```python
          def add(a: int, b: int) -> int:
              return a + b
          ```

          2. **Consider Generics for Flexibility** - If the function should support multiple numeric types:
          ```python
          from typing import Union

          def add(a: Union[int, float], b: Union[int, float]) -> Union[int, float]:
              return a + b
          ```
          Or using Python 3.10+ syntax:
          ```python
          def add(a: int | float, b: int | float) -> int | float:
              return a + b
          ```

          3. **Missing Docstring** - Consider adding one:
          ```python
          def add(a: int, b: int) -> int:
              """Returns the sum of two integers."""
              return a + b
          ```

          **Summary of Recommendations:**
          - ✅ Add type annotations (critical)
          - ✅ Add a docstring
          - ✅ Decide on intended type scope (`int` only vs. broader numeric support)

          The logic itself is correct and clean.
        type: text
      id: msg_011CdTHe7NuXrgpyBkiVBGNu
      model: claude-sonnet-4-6
      role: assistant
      stop_details: null
      stop_reason: end_turn
      stop_sequence: null
      type: message
      usage:
        cache_creation:
          ephemeral_1h_input_tokens: 0
          ephemeral_5m_input_tokens: 0
        cache_creation_input_tokens: 0
        cache_read_input_tokens: 0
        inference_geo: global
        input_tokens: 59
        output_tokens: 313
        service_tier: standard
    status:
      code: 200
      message: OK
version: 1
