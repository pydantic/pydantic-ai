"""CodeMode Examples.

Available Examples:
    - batch_operations.py: Create multiple calendar events using code mode (no external deps)
    - follow_the_money.py: Fraud ring detection via transaction graph traversal (no external deps)
    - pr_comment_buckets.py: PR discussion intensity analysis via GitHub MCP (requires GITHUB_PERSONAL_ACCESS_TOKEN)

Run:
    uv run -m pydantic_ai_examples.code_mode.batch_operations
    uv run -m pydantic_ai_examples.code_mode.follow_the_money
    uv run -m pydantic_ai_examples.code_mode.pr_comment_buckets
"""
